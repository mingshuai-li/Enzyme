# Run the Rust scripts

To run the rust scripts, just with
```Rust
# rosenbrock forward iteration
cargo +enzyme run --example rosenbrock_fwd_iter --release

# or all of them
cargo +enzyme test --examples
```
